# устанавливает объекту obj атрибут name со значением value.
# Если объект obj уже имеет атрибут name, его значение перезаписывается.
# если атрибут существует то просто поменяется значение на новое

# принимает три аргумента:
# obj — объект
# name — имя атрибута
# value — значение атрибута


# class Cat:
#     pass
#
# cat = Cat()
# cat.name = 'Кемаль'
# setattr(cat, 'age', 1)
# setattr(cat, 'name', 'Роджер')
#
# print(cat.age)           # 1
# print(cat.name)          # Роджер
