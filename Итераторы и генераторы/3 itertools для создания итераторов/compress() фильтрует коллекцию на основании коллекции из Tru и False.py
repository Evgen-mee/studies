# использует значения другого итерируемого объекта для индикации того,
# следует ли принять значение или игнорировать его.

# from itertools import compress
# data = 'ABCDEF'
# selectors = [True, False, True, False, True, False]
# result = compress(data, selectors)
# print(list(result))
# выводит:
# ['A', 'C', 'E']