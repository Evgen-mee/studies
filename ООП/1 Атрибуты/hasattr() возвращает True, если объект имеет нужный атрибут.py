# возвращает True, если объект obj имеет атрибут name, или False в противном случае.

# принимает два аргумента:
# obj — объект
# name — имя атрибута


# class Cat:
#     night_vision = True
#     paws_count = 4
#
# cat = Cat()
# cat.name = 'Кемаль'
#
# print(hasattr(cat, 'name'))            # True
# print(hasattr(cat, 'age'))             # False
# print(hasattr(cat, 'night_vision'))    # True
